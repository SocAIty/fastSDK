import os

# ALLOWS TO SET API KEYS WITH ENVIRONMENT VARIABLES
# If not specified with the init of @fastSDK or in the service-client definition, these API keys are used.
# they should be in form {service_url_nickname: api_key}
API_KEYS = {}
